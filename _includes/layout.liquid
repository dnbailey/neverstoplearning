<!DOCTYPE html>
<html lang="en">
  <body>
    {% if title %}
    <h1>{{ title }}</h1>
    {% endif %} {% if date %}
    <p>{{ date | date: "%a, %b %d, %Y" }}</p>
    {% endif %}

    {{ content }}
  </body>
</html>
